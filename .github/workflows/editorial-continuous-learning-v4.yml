name: Retired editorial v4 marker

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Workflow v4 đã được thay thế bởi Editorial quality gate v5."
